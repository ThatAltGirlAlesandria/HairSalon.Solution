@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h2>Are you sure you want to delete this client? This action can't be undone once completed.</h2>

@Html.DisplayNameFor(model => model.client_name): @Html.DisplayFor(model => model.client_name)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}
@Html.ActionLink("Client list", "Index")